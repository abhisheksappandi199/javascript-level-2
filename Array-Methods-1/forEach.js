// foreach - method executes a      provided fn once for each element in array
// eacg ele is passed as argument to the function

const products = ['a','b','c']

products.forEach((e,index , arr) => console.log(e ,index , arr))