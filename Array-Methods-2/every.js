// every : returns true if all elements pass the given condition.  Otherwise returns false

// ex : check all the numbers in arr greater than 100
const price = [125,175,200,250]

const result = price.every(e => e > 100)
console.log(result);