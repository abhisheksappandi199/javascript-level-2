// map : returns a new array with results of calling a provided function on every element on thr array

const tech = ['js','node','react','angular']

const result = tech.map(e => e.toUpperCase())
console.log(result);